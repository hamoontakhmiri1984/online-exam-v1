من پیشنهادم برای Exam Lifecycle (قبل از نگاه به کد)

سوال اصلی سناریو ۳ اینه: Draft → Published → Scheduled → Finished یعنی چهار وضعیت متفاوت، ولی طبق چیزی که تو Scenario #2 دیدیم، مدل Exam تو schema اصلاً فیلد status نداره — فقط scheduledAt + durationMinutes رو ذخیره می‌کنه و status ('completed'/'upcoming') را derived حساب می‌کنه.

این یعنی مدل فعلی فقط ۲ حالت رو می‌تونه نشون بده (تموم‌شده / نشده)، نه ۴ تا. پس یا:

گزینه‌ی الف — این ۴ حالت اصلاً business requirement واقعی نیستن
شاید طراح تصمیم گرفته "Draft" و "Published" مفهومی فرانت‌اند/UX باشن (مثلاً یه امتحان که هنوز سوال نداره یا instructor هنوز save نکرده) و اصلاً نیازی به state machine واقعی سمت بک‌اند نیست — همین که امتحان scheduledAt داره یعنی "Scheduled"، و اگه هنوز create نشده که "Draft" یعنی صرفاً یه فرم خالی سمت کلاینته.

گزینه‌ی ب — این یه gap واقعیه
اگه واقعاً business logic نیاز داره که یه Instructor بتونه امتحان رو "بسازه ولی هنوز به دانشجوها نشون نده" (Draft)، این با مدل فعلی غیرممکنه — چون همین که POST /exams بزنه و به گروه‌هایی وصلش کنه، همون لحظه هر Student عضو اون گروه‌ها می‌تونه از طریق GET /exams ببینتش (چیزی که تو exams.ts که همین الان نوشتم هم صادقه — فیلتر فقط بر اساس عضویت گروهیه، نه یه فیلد "publish شده یا نه").

من قبل از نگاه به بقیه‌ی کد (client pages، آیا فرانت واقعاً از این ۴ حالت استفاده می‌کنه یا نه) نمی‌تونم مطمئن بگم کدوم گزینه درسته. برم چک کنم فرانت (ExamsPage یا مشابه) و ببینم واقعاً این lifecycle جایی استفاده می‌شه یا صرفاً تو توضیحات اولیه‌ی خودت یه future requirement بوده که هنوز پیاده نشده.

پیش‌بینیم درست بود — رفتم سراغ فرانت (useExamData.ts تو ExamRunnerPage) و یه یافته‌ی واقعی و مهم پیدا کردم:

یافته: نشتِ محتوای امتحان قبل از موعد (مربوط به Lifecycle)

useExamData همون لحظه که صفحه‌ی اجرای امتحان mount می‌شه، هم‌زمان getExamById و getQuestionsByExamId رو صدا می‌زنه — قبل از این‌که دانشجو دکمه‌ی Start رو بزنه. سمت سرور (questions.ts) هم هیچ محدودیت زمانی نداره: تنها شرطش عضویت گروهیه (loadAccessibleExam)، نه اینکه آیا scheduledAt رسیده یا نه.

نتیجه: یه دانشجو می‌تونه همون لحظه که Instructor امتحان رو ساخت (حتی اگه قرار باشه هفته‌ی بعد برگزار بشه)، بره تو صفحه‌ی اجرا و متن کامل همه‌ی سوالات و گزینه‌ها رو (بدون جواب صحیح) از قبل ببینه. این دقیقاً همون چیزیه که «Scheduled» باید جلوشو بگیره ولی نمی‌گیره.

Draft/Published که تو تست‌پلن اولیه‌ات خواسته بودی هم واقعاً هیچ‌جای کد (نه فرانت نه بک‌اند) پیاده نشده — فقط completed/upcoming derived وجود داره. ولی مسئله‌ی اصلی این نبود؛ مسئله همون timing-gate روی محتوای سوالاته.

اصلاح: یه چک زمانی رو، هم تو GET /exams/:examId/questions (برای Student) و هم تو POST /exams/:examId/attempts/start، اضافه می‌کنم: قبل از scheduledAt، نه سوالات نشون داده بشن نه اجازه‌ی start.

---

## ✅ وضعیت: حل‌شده (تایید شده روی کد فعلی)

چک کردم — این fix از قبل پیاده شده، و حتی از پیشنهاد بالا قوی‌تره:

1. **`server/src/routes/questions/examQuestions.routes.ts`** (خط ۶۴ و ۱۲۲): تو `GET /exams/:examId/questions` و `GET /exams/:examId/questions/count`، اگه `role === 'Student'` و `Date.now() < exam.scheduledAt`، آرایه‌ی خالی / `count: 0` برمی‌گرده.
2. **لایه‌ی اضافه‌ی دیگه** که تو پیشنهاد اولیه نبود: حتی بعد از `scheduledAt`، دانشجو تا وقتی یه `ExamAttempt` واقعی (یعنی `/start` زده باشه) نداشته باشه، بازم متن/گزینه‌ی سوال‌ها رو نمی‌بینه (فقط بعد از start) — وگرنه می‌شد بعد از رسیدن زمان و بدون start کردن (بدون تایمر فعال)، سوالات رو خوند.
3. **`server/src/routes/examAttempts.ts`** (خط ۶۸): تو `POST /start` هم مستقل همون چک `Date.now() < exam.scheduledAt` هست («دفاع در عمق» - چون یکی می‌تونه مستقیم به این endpoint درخواست بزنه، بدون رد شدن از UI/چک بالا).

نتیجه: نشتِ محتوای امتحان قبل از موعد کاملاً بسته‌ست. Draft/Published هم (طبق گزینه‌ی الف بالا) هیچ‌وقت پیاده نشد و لازم هم نبود - چون مسئله‌ی واقعی همون timing-gate بود که الان حل شده.
